ext {
    //创建了一个名为android的，类型为map的变量
    // groovy中可以用[]来创建map类型。那么就是一个map下面又创建了一个map，且名字叫做android。
    android = [
            // compileSdkVersion:29,
            minSdkVersion   : 19,
            targetSdkVersion: 29
    ]
}
